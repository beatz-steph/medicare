import React from 'react';

// import './btn.styles.scss';
import { BtnMain } from './btn.styles';

const Button = ({ placeholder }) => {
	return <BtnMain placeholder={placeholder} />;
};

export default Button;
